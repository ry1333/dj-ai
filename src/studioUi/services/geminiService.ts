import { Track } from '../types';

// Stub implementation - to be integrated with Gemini API later
export async function generatePlaylist(vibe: string): Promise<Track[]> {
  // Mock implementation - returns empty array for now
  console.log('generatePlaylist called with vibe:', vibe);
  return [];
}
