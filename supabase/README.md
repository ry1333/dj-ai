# Supabase Setup (local/remote)
1) Install Supabase CLI (or use the dashboard).
2) Apply migration `0001_init.sql`.
3) Ensure RLS is ON for `posts` and `reactions`.
4) Create bucket for audio in Storage (if using Storage).
